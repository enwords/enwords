<%= form_for(rus_sentence) do |f| %>
  <% if rus_sentence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rus_sentence.errors.count, "error") %> prohibited this rus_sentence from being saved:</h2>

      <ul>
      <% rus_sentence.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %>
    <%= f.number_field :id %>
  </div>

  <div class="field">
    <%= f.label :sentence %>
    <%= f.text_field :sentence %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
